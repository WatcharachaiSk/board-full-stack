const api = '/api'

export enum ApiPaths {
  Login = `${api}/auth/login`,
  Post = `${api}/post`,
  PostByUser = `${api}/post/user`,
  Community = `${api}/community`,
  Comment = `${api}/comment`,

}
